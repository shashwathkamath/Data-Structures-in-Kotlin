fun flatten(root: RandomNode?): RandomNode? {

    //     //I am using random node, consider random node as child node as per question
    //     var current = root

    //     while(current!=null){
    //         if (current.random!=null){
    //             val childHead = current.random
    //             val childTail = getTail(childHead)
    //         }
    //         current = current.next
    //     }
    //     return root
    // }

    // fun getTail(head:RandomNode?):RandomNode?{
    //     if (head == null) return null
    //     var current = head
    //     while(current!=null){
    //         if (current.random!=null){
    //             val tail = getTail(current.random)
    //             tail?.next = current.next
    //             tail?.next.prev
    //         }x
    //     }

    return null
}
