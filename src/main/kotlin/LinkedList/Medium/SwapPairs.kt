fun swapPairs(head: ListNode?): ListNode? {
    val dummy: ListNode? = ListNode(0)
    dummy?.next = head
    var prev = dummy
    var current = head

    while (current?.next != null) {
        v
    }

    return dummy?.next
}
