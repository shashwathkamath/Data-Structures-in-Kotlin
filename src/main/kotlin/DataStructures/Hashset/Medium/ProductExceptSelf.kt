fun selfExcludedArrayProduct(nums: IntArray) {
    // val n = nums.size
    // val ans = IntArray(n) { 1 }

    // for (i in 1 until n) {
    //     ans[i] = ans[i - 1] * nums[i - 1]
    // }

    // for
}
