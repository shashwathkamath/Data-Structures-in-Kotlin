{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "java",
            "request": "launch",
            "name": "Run Kotlin with Gradle",
            "tasks": [
                "build",
                "run"
            ],
            "cwd": "${workspaceFolder}"
        }
    ]
}